name: BF-652 BF-649 Phrase Fix

on:
  push:
    branches:
      - bf-652-pair-current-add-drop
    paths:
      - .github/workflows/bf652-bf649-phrase-fix.yml

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
        with:
          ref: bf-652-pair-current-add-drop
      - name: Apply BF-649 phrase fix
        run: python .github/bf652_bf649_phrase_fix.py
      - name: Commit phrase fix
        shell: bash
        run: |
          git config user.name "Matt"
          git config user.email "323053860+matthewbutler01-netizen@users.noreply.github.com"
          git add scripts/butler-dashboard.ps1
          git commit -m "BF-652: preserve BF-649 literal ranking contract"
          git push origin HEAD:bf-652-pair-current-add-drop
